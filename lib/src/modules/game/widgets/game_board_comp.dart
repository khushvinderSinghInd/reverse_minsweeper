import 'package:flutter/material.dart';

class GameBoardComp extends StatelessWidget {
  const GameBoardComp({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}